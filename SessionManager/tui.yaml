# ── SessionManager TUI manifest ──────────────────────────────────────────────
manifest:
  name: Session Browser
  description: Full interactive TUI with split panels, detail view, and bulk operations
  script: scripts/sm-tui.py
  icon: "\U0001F5A5"
  modes:
    default:
      name: Interactive Browser
    simple:
      name: Simple Selector
      args: [--no-tui]
