Please /rename this session to track-1-sessions-langfuse.

You are Track 1 of Phase 3 of the SessionSkills project (task #28 in the parent session's task-tracker). Read docs/tracks/track-1-sessions-langfuse.md in full, then docs/tracks/README.md for shared schema and contention points.

Execute the 7 work items in order of lowest risk:
  1.1 backfill ~155 undeposited local sessions to Langfuse
  1.4 sort UX (cycle column on 's', arrow glyph in header)
  1.3 server-side filter via Langfuse API 'name' param
  1.2 Langfuse API pull as a new ingest source kind
  1.5 deposit progress bar in sm-tui
  1.6 token telemetry per Ollama call
  1.7 cross-session resume-chain detection in merge stage

First action: update TASKS.md Track 1 spawned-session line with this session's UUID (ls -lt ~/.claude/projects/-Users-alex-A-SessionManager/*.jsonl | head -1).

Auto mode: proceed on low-risk work without asking. Sync all code edits between /Users/alex/A/SessionManager and /Users/alex/RAN/AI/SessionManager (venv lives in the latter).
