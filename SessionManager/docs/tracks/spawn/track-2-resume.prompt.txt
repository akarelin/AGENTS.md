Please /rename this session to track-2-projects-sessions-resume.

You are resuming Track 2 of Phase 3 of SessionSkills, picking up where session
66afd881-8b89-4e24-bd00-64bdb7704dae left off. Read docs/tracks/track-2-projects-sessions.md
(spec) and docs/tracks/README.md (cross-track shared infra), and especially
docs/tracks/track-2-handoff.md for what's already landed, what's pending, and
the gotchas you need to know.

Already done (verify against the files, then proceed):
  2.1 canonical project registry  → ~/_/KG/Project/_registry.yaml (39 projects)
  2.2 resolver library            → /Users/alex/A/SessionManager/sessions/projects.py
  2.3 link_project pipeline stage → sessions/stages/link_project.py + wired into
                                    orchestrator.py + sessionskills.yaml
                                    (verify: `sm-pipeline run link_project` returns
                                     processed=0 skipped=N — idempotent)
  2.9 shared skill                → /Users/alex/SD.agents/skills/projects-sessions/SKILL.md
                                    (instruction-style, no scripts; uses sessions.projects
                                     directly via short bash one-liners)

Remaining, in order:
  2.7 work-atoms classify.py — prefer canonical_project over its own LLM call.
      The skill currently lives at /Users/alex/_/{internals}/Skills/work-atoms/
      (NOT yet migrated to SD.agents/skills/ — that's task #15 in TASKS.md).
      Edit it in place; the migration will move the patched copy.
  2.4 Obsidian bidirectional view — write sessions/stages/_obsidian_emit.py and
      hook it from link_project's --emit-obsidian flag (already wired). Render
      a `## Sessions` block on each project's obsidian_page; render `## Task tree`
      block when the entry has tasks: (e.g. sessionmanager).
  2.6 TUI Projects view (key 3) — narrow additive change to scripts/sm-tui.py
      to source rows from the canonical registry instead of (or in addition to)
      the Langfuse-tag aggregation. NARROW; sync to /Users/alex/RAN/AI/SessionManager
      (sm-tui actually runs from there per bin/_bootstrap.sh).
  2.5 Unlinked-project review flow — extend sessions/review_tui.py to handle
      the new kind='unlinked_project' items (link_project already enqueues them).
  2.8 task-hierarchy schema — registry already carries a tasks: block under
      sessionmanager; resolver already returns task_id and link_project writes
      task_id + task_parent_path. Remaining: render task tree on Obsidian pages
      (in 2.4's emitter), and reduce TASKS.md to a one-line pointer to the
      registry.

Critical gotchas:
  • sessions/ Python package lives ONLY in /Users/alex/A/SessionManager. The
    bootstrap (bin/_bootstrap.sh) finds it there; do NOT mirror sessions/ to
    /Users/alex/RAN/AI/SessionManager — that copy is the older standalone code.
  • scripts/sm-tui.py IS sync'd between both repos and runs from /RAN — so
    sm-tui edits MUST be written to both locations.
  • The work-atoms skill is at /Users/alex/_/{internals}/Skills/work-atoms/
    (literal `{internals}` in the path), NOT under SD.agents/skills/.
  • Auto mode. Don't touch sm-tui.py or session-to-langfuse.py except for
    narrow additive columns.
  • Track 1 (c5ef017d) and Track 3 (b1c418ff) are running in parallel and may
    have written to TASKS.md or other shared files since the handoff was
    created — re-read shared files before editing.

First action: update TASKS.md Track 2 spawned-session line with THIS session's
UUID (the handoff session, not the original).
