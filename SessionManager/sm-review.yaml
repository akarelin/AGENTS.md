# ── SessionSkills HITL Review TUI manifest ──────────────────────────────────
manifest:
  name: HITL Review
  description: Walk pending low-confidence items in the SessionSkills review queue
  script: scripts/sm-review.py
  icon: "✅"
  modes:
    default:
      name: Interactive Review
    notify:
      name: Notify — count only
      args: [--notify]
      inline: true
